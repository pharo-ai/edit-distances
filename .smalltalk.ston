SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'AIEditDistances',
      #directory : 'src'
    }
  ],
   #testing : {
    // Adding this classes because we have defined test as extensions on those classes
    #include : {
      #classes : [ #ObjectTest, #ArrayTest, #SetTest ]
    }
  }
}
